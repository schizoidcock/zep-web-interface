# Zep Web Interface Configuration
# Copy this file to .env and configure for your environment

# Required Configuration - MUST BE SET
# ZEP_API_URL should be just the hostname (http:// prefix will be added automatically)
ZEP_API_URL=
ZEP_SERVER_PORT=
ZEP_API_KEY=

# Server Configuration
HOST=0.0.0.0
PORT=8080
TLS_ENABLED=false

# Proxy Configuration (optional)
# PROXY_URL=http://proxy-server:8080
PROXY_PATH=/admin
TRUST_PROXY=true
CORS_ORIGINS=*

# Production Examples:
# Railway: ZEP_API_URL=zep-server.railway.internal
# Heroku: ZEP_API_URL=https://your-zep-app.herokuapp.com (include https:// for external URLs)
# Docker: ZEP_API_URL=zep-server
# IPv6 local: ZEP_API_URL=[::1]